
  # Litera web app design

  This is a code bundle for Litera web app design. The original project is available at https://www.figma.com/design/WffV2rOckxv8cVcpQrnemb/Litera-web-app-design.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  